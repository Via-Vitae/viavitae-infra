---
galaxy_info:
  author: ViaVitae platform team
  description: Install and register the pinned GitHub Actions runner that carries the [self-hosted, linux, x64, eu-infra] label, so security.yml and deploy.yml jobs have an EEA execution target.
  company: ViaVitae IT Technologies
  license: proprietary
  min_ansible_version: "2.17"
  platforms:
    - name: Debian
      versions:
        - bookworm
  galaxy_tags:
    - infrastructure
    - ci
    - viavitae
  namespace: viavitae

dependencies:
  - common
